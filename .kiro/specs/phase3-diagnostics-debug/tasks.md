# Implementation Plan

## Tasks

- [x] 1. 診断契約と設定拡張を共有スキーマに定義する
  - 診断パネル用のアドレス定数(スナップショット要求・応答・各表示ウィジェット・ログ削除操作)を内部観測名前空間に追加する
  - 送受信メッセージ記録・診断スナップショット・サブネット判定結果のスキーマを定義する(blob は長さのみ保持、長い文字列は打ち切りフラグ付き)
  - 実行時設定にリングバッファ件数・喪失率ウィンドウ・NDJSON 出力先・ログ容量閾値を optional + 既定値付きで追加し、案件差分をデータで表現できるようにする
  - 完了条件: 既存の設定ファイルが無変更のまま valid であることと、診断スキーマの妥当性がスキーマ単体テストで緑になる
  - _Requirements: 2.1, 2.2, 2.6, 3.1, 3.4, 5.3, 7.3_

- [x] 2. 診断ドメインの純粋部品を実装する
- [x] 2.1 (P) 直近 N 件を保持するリングバッファを実装する
  - 容量固定で最古破棄・挿入順取り出しの汎用バッファを純粋ロジックとして実装する
  - 容量境界(N 到達前後・最古破棄・取り出し順序)を単体テストで検証する
  - 完了条件: 上限超過時に最古の記録から破棄され最新要素が必ず残ることがテストで緑になる
  - _Requirements: 2.1, 2.2, 6.2_
  - _Boundary: RingBuffer_

- [x] 2.2 (P) 宛先サブネットの静的判定を実装する
  - 宛先ホスト文字列と注入されたインターフェース情報のみから「同一ホスト/同一サブネット/別サブネットの疑いあり/判定不能(理由付き)」を返す純関数を実装する
  - ループバックは同一ホスト扱い、ホスト名・IPv6 宛先・IPv4 インターフェースなしは理由付きの判定不能とし、ネットワーク送信を一切行わない
  - 同一サブネット/別サブネット(TEST-NET 帯含む)/ループバック/自インターフェース一致/判定不能各種/複数 NIC/狭いマスク境界をテーブル駆動の単体テストで網羅する
  - 完了条件: 実ネットワーク構成に依存しない入力の組み合わせテストが全パターン緑になる
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 6.4_
  - _Boundary: SubnetCheck_

- [x] 2.3 (P) 喪失率ウィンドウと到達性導出を実装する
  - 確定した ping 結果の直近 W 件窓で観測数・喪失数・喪失率を集計する部品を実装する(観測 0 件時は率なし)
  - 既存 ping 監視のスナップショットから到達性(未確立/到達/喪失)を導出する純関数を実装する(連続喪失 1 以上で喪失、ping/pong 仕様自体は無変更で利用)
  - 窓未満・窓超過の押し出し・率なし・到達性の 3 状態遷移を単体テストで検証する
  - 完了条件: 喪失率と到達性の判定が仕様どおりであることがテストで緑になる
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 6.2_
  - _Boundary: LinkHealth_

- [x] 2.4 (P) ログ容量の集計と削除対象選定を実装する
  - ファイル一覧(名前・サイズ・更新時刻)と閾値から合計サイズ・超過判定を決定的に返す純関数を実装する
  - 削除対象の選定を「使用中ファイルを除外し、古い順に累計が閾値の 90% に達するまで(または対象が尽きるまで)」の規則で実装する
  - 容量集計・超過判定・選定規則(使用中除外・停止条件・対象枯渇)を単体テストで検証する
  - 完了条件: 同一入力に対し決定的な選定結果が返り、使用中ファイルが結果に含まれないことがテストで緑になる
  - _Requirements: 2.6, 2.7, 6.2_
  - _Boundary: NdjsonQuota_

- [x] 3. 診断ドメインの IO 部品を実装する
- [x] 3.1 (P) NDJSON 逐次書き出しを実装する
  - メッセージ記録を 1 件 1 行の JSON で起動ごとの新規ファイルへ追記する部品を、ファイルシステム操作を注入可能にして実装する
  - ディレクトリ作成失敗・ストリームエラー時はエラーを 1 回ログして degraded(以後 no-op)化し、例外を呼び出し元へ漏らさない
  - 正常追記(1 件 1 行・記録スキーマ適合)と失敗時 degraded を単体テストで検証する
  - 完了条件: 書き出し失敗を注入しても throw されず、以後の追記が no-op になりエラーログが 1 回だけ出ることがテストで緑になる
  - _Requirements: 2.3, 2.5, 6.2_
  - _Boundary: NdjsonWriter_

- [x] 3.2 (P) 100ms 間引きのパネル反映部品を実装する
  - dirty フラグ方式で、状態変化時に印を付け 100ms tick で dirty のときだけ全診断項目を表示専用経路で送出する部品を実装する
  - 表示内容(到達性・RTT・喪失率・サブネット判定・ログ使用量・直近メッセージの整形行)への整形を担い、Unity への OSC 送信を発生させない
  - fake timer で「連続 dirty でも tick あたり 1 バースト」「dirty なし tick は無送出」「破棄後は停止」を単体テストで検証する
  - 完了条件: 100ms 窓あたりの送出バーストが最大 1 回であることがテストで緑になる
  - _Requirements: 5.3, 5.4, 5.6, 6.2_
  - _Boundary: DiagPanelSink_

- [x] 4. 診断ファサードで部品を合成する
- [x] 4.1 記録イベントとスナップショット供給を実装する
  - リングバッファ・NDJSON 書き出し・喪失率窓・パネル反映を合成し、送受信記録・ping 周期・pong 採用の各イベントを受けて一括更新と dirty 通知を行うファサードを実装する
  - 生成時にサブネット判定を 1 回評価し、OSC 引数から記録形式への変換(blob の長さ化・文字列打ち切り)を所有する
  - 全公開メソッドが例外を外へ漏らさず、内部でログして継続することを保証する
  - 記録イベントから各部品への連動・変換規則・スナップショットのスキーマ適合を単体テストで検証する
  - 完了条件: 記録イベント投入後のスナップショットが診断スキーマに適合し、各部品の状態が連動していることがテストで緑になる
  - _Requirements: 1.2, 2.1, 2.3, 3.1, 4.1, 5.3, 6.2_

- [x] 4.2 ログ容量監視と削除実行を実装する
  - 起動時と 60 秒間隔でログディレクトリの合計サイズを集計し、非超過から超過への遷移時に 1 回だけブラウザへトースト通知を送り、スナップショットの警告表示を更新する
  - 削除実行では選定規則に従って古いログを削除し、失敗はログして継続、完了直後に使用量を即時再集計してパネル警告を解消する
  - fake timer で「超過遷移時のみ通知 1 回」と「削除実行・即時再集計」を単体テストで検証する
  - 完了条件: 閾値超過の遷移で通知がちょうど 1 回発生し、削除後に周期を待たず警告が解消されることがテストで緑になる
  - _Requirements: 2.6, 2.7, 6.2_

- [x] 5. ランタイムへ診断を統合する
- [x] 5.1 デバッグモード分岐と記録フックを組み込む
  - 設定の debug フラグが真のときだけ診断エンジンを生成し、起動時に有効/無効をログ出力する(無効時はエンジン・タイマー・NDJSON を一切生成しない)
  - 送信記録は module 発(送信関数のラップ)とウィジェット発(送信フィルタ内フック)の 2 系統、受信記録は受信フィルタ先頭のフックで行い、いずれも null ガード内に限定して無効時のホットパスコストを残さない
  - 内部観測名前空間のメッセージは記録対象から除外し、ping 送信時の連続喪失差分から喪失確定を導出してエンジンへ通知する(既存 ping/pong・manifest の制御フローは無変更)
  - debug 無効時のエンジン不生成・送信素通し・NDJSON 不生成と、有効時の送受信両方向の記録フック発火・喪失導出を単体テストで検証し、既存テストが無変更で緑のままであることを確認する
  - 完了条件: debug OFF で診断処理が一切実行されず既存機能が従来どおり動くこと、ON で記録が両方向に発生することがテストで緑になる
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.4, 3.5, 5.5, 6.2_

- [x] 5.2 診断観測点と名前空間ガードを実装する
  - スナップショット要求の受信時、デバッグ有効ならスナップショットを JSON で観測点アドレスへ返信し、無効時は既存の内部名前空間一括破棄に落とす(応答なし)
  - 送信フィルタに内部観測名前空間の恒常的な外向き遮断(抑制付き警告ログ)を実装し、通知・削除操作を含む診断系メッセージが Unity へ届かないことを構造的に保証する
  - 削除ボタン発のメッセージは遮断前に削除実行の指示として処理してから破棄する
  - 観測点応答・削除操作の実行と破棄・外向きガードを単体テストで検証する
  - 完了条件: 診断系アドレスが外向きに送出されず、削除操作が実行されつつ Unity へは届かないことがテストで緑になる
  - _Requirements: 2.7, 2.8, 5.6, 6.2_

- [x] 6. mock-unity に故障注入を実装する
- [x] 6.1 (P) 応答フィルタによる故障注入モードを実装する
  - 応答生成ロジックは無変更のまま、返信リストへの出口フィルタとして 5 モード(pong 停止・全応答停止・確率喪失・応答遅延・不正応答)を実装する
  - 確率喪失は E2E の再現性のため乱数でなく率から導出した決定的巡回パターンで抑止し、全応答停止時も受信計数は継続させる
  - 各モードの応答挙動(抑止対象・遅延指示・不正バイト列置換)と受信計数継続を単体テストで検証する
  - 完了条件: 5 モードすべての応答フィルタ挙動がテストで緑になる
  - custom module 側のタスク群(2〜5)とはパッケージが異なり独立に実施可能
  - _Requirements: 6.1, 6.2_
  - _Boundary: MockUnityFault_

- [x] 6.2 故障モードの CLI 指定と起動情報出力を実装する
  - CLI フラグでモードとパラメータ(喪失率・遅延ミリ秒)を指定できるようにし、既存引数の後方互換を維持する
  - 遅延モードの応答をタイマーで遅延送出し、READY 行の JSON に故障モード情報を含めてテストから起動状態を確認できるようにする
  - 完了条件: 故障モード付きで起動した READY 行にモード情報が出力され、指定モードが応答に反映される
  - _Requirements: 6.1_

- [x] 7. (P) 診断パネルのレイアウトと debug 用 config を作成する
  - 診断パネルを通常レイアウトとは別ファイルの fragment として作成し、表示専用ウィジェット群(到達性・RTT・喪失率・サブネット判定・ログ使用量・直近メッセージ)と削除ボタン(明示 target 必須)を配置する
  - 通常レイアウトに既定で閉じたモーダル + fragment 参照を追加して合流させる(既存パネル・動的コンテナは不変。fragment で表示できない場合は代替案へフォールバックし判断を記録する)
  - debug 有効のサンプル config を追加し、NDJSON 出力既定先を VCS 追跡から除外する
  - 完了条件: headless 起動でモーダル内に診断パネルが表示され、既存レイアウトの挙動が変わらないことを目視確認できる
  - タスク 6 と並行可能(触れるファイルが重ならない)
  - _Requirements: 5.1, 5.2, 7.2, 7.3_
  - _Boundary: DiagnosticsLayout_
  - _Depends: 1_

- [x] 8. E2E テストで診断機能を検証する
  - 同一の E2E スイートへ順に追記するため 8.1〜8.3 は直列で実施する
- [x] 8.1 正常系とデバッグ無効の E2E を実装する
  - debug config での起動で診断パネルに到達・RTT 数値・喪失率 0%・同一ホスト判定が表示され、NDJSON が生成され各行が記録スキーマでパースできることを検証する
  - 既定 config での起動でスナップショット要求に応答がなく NDJSON 出力先が生成されないことを検証し、既存 Phase 1/2 E2E の緑を回帰として確認する
  - 完了条件: 正常系・無効系の E2E が headless ループバック構成で緑になる
  - _Requirements: 1.2, 2.3, 2.4, 5.5, 6.3_

- [x] 8.2 故障注入系の E2E を実装する
  - pong 停止で到達性が喪失・喪失率上昇となること(echo 系は応答継続)、プロセス停止(全断)で喪失表示へ遷移することをポーリングで検証する
  - 確率喪失で喪失率が 0% より大きく 100% 未満になること、応答遅延で RTT 表示が指定値以上になることを検証する
  - 不正応答の注入で custom module が停止せず、診断パネル更新と既存機能が継続することを検証する
  - 完了条件: 喪失・切断・確率喪失・遅延・不正応答の 5 シナリオの E2E が緑になる
  - _Requirements: 6.3, 6.7_

- [x] 8.3 別サブネットとログ容量の E2E を実装する
  - E2E プロセスハーネスに環境変数指定オプションを追加する(省略時は従来挙動を維持)
  - TEST-NET 帯を宛先にした一時 config を環境変数経由で渡して起動し、パネルに「別サブネットの疑いあり」が表示されることを検証する
  - 小さな閾値の一時 config とダミーの旧 NDJSON を事前配置して起動し、使用量警告の表示と、削除ボタン押下の模擬による古いログの削除・警告解消を検証する
  - 完了条件: 別サブネット判定とログ容量警告・削除の E2E が緑になる
  - _Requirements: 2.6, 2.7, 6.5_

- [ ] 9. 手動検証手順を追記し全体回帰を確認する
  - 検証手順書に「debug ON でのパネル目視確認」「NDJSON の目視確認」「debug OFF での無変化確認」の手順と長時間デバッグ時の容量に関する運用注意を追記する
  - vendor(lockfile 含む)に差分がないことを確認し、テスト一式(単体 + E2E)が緑であることを確認して Phase 進捗を更新する
  - 完了条件: テスト一式が緑、vendor 無改造が git 差分で確認でき、手動検証手順が追記されている
  - _Requirements: 6.6, 7.1_
