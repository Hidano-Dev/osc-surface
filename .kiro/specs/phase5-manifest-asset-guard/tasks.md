# Implementation Plan

## Tasks

- [x] 1. 共有スキーマにプロジェクト識別子とガード契約を定義する
  - マニフェストスキーマ(version 1 のまま)へ空でない必須のプロジェクト識別子を追加し、識別子の欠落・空文字・非文字列がスキーマ検証で拒否されるようにする(OSC 1.0 の `s` 型 JSON ペイロード内フィールドであり型タグは不変)
  - 実行時設定スキーマへ期待識別子(任意・空文字不可)を追加する
  - ガード拒否イベントの記録スキーマと NDJSON 行の総和型(既存メッセージ記録行の形式は不変。行種別は kind フィールドの有無で判別)、診断パネルのガード行アドレス定数を新設する
  - shared 単体テストで識別子欠落・空文字・不正型の拒否、ガードレコードの受理/拒否、期待識別子の任意性と空文字拒否を検証する
  - 注意: 本タスク単独では mock-unity と既存 E2E が壊れる中間状態になる(update-together バッチ A〜D)。全体テストの緑はタスク 4・5.2 で回収し、本タスクの完了条件にしない
  - 完了条件: shared パッケージの単体テストが単独で緑になり、同一入力に対する検証結果が決定的である
  - _Requirements: 2.1, 2.2, 2.4, 3.1, 5.6_

- [ ] 2. custom module に誤接続ガードを実装する(バッチ C)
- [x] 2.1 採否判定の単一関門へ識別子照合を追加する
  - JSON パース → スキーマ検証 → 識別子照合の 3 段構成とし、照合はスキーマ検証の通過後に行う(スキーマ違反は従来どおりの拒否理由とし識別子起因と混同しない)
  - 期待識別子が未設定のときは照合段をスキップし従来動作とする。照合は正規化なしの厳密文字列比較とする
  - 不一致時は期待値・受信値付きの専用拒否理由で拒否し、内部状態と採用済みマニフェストを変更しない(採用済み中は再要求が発生せず、要求中は再送が継続する)
  - 重複抑制キーは拒否理由と期待/受信の組を含み、別の不一致元が現れたら抑制が解け、正しいマニフェスト採用で抑制がリセットされる
  - 単体テストで一致採用・不一致拒否・未設定スキップ・状態不変・抑制キー規則を検証する
  - 完了条件: 採否判定コンポーネントの単体テストが単独で緑になる
  - _Requirements: 2.3, 3.2, 3.3, 3.4, 3.5, 3.6, 4.2_

- [x] 2.2 (P) NDJSON 基盤をガード記録に対応させる
  - 追記レコードを既存メッセージ記録とガード拒否記録の総和型に拡張する(既存行の直列化形式は不変)
  - ストリーム生成とディレクトリ作成を初回追記まで遅延し、イベントゼロの起動では空ファイルを作らない
  - 削除(purge)の保護対象を複数ファイル名へ拡張し、診断エンジンへ外部の保護ファイル名を受け渡せるようにする
  - 既存単体テストを更新し、総和型の直列化・遅延オープン・複数ファイル保護を検証する
  - 完了条件: NDJSON 書き出し・容量管理・診断エンジンの単体テストが単独で緑になる
  - _Requirements: 4.1_
  - _Boundary: NdjsonWriter / NdjsonQuota / DiagnosticsEngine_
  - _Depends: 1_

- [x] 2.3 ガード拒否の常時記録とパネル発行部品を実装する
  - debug 設定に依存せず常時稼働する部品として、拒否イベント(理由・期待識別子・受信識別子・送信元)を専用 NDJSON ファイルへ追記し、診断パネルのガード行を表示専用経路で発行する
  - 非リピート時のみ NDJSON 1 行追記と console エラー 1 回。リピート時は追記せず累計回数の表示のみ更新する(氾濫させず観測は途切れさせない)
  - セッション開始時の再発行用のクライアント別発行、purge 保護用の現行ファイル名取得、破棄を提供する
  - 記録失敗(fs 例外)は degrade 方式で握りつぶし、採否判定と UI 継続を妨げない
  - 単体テストで非リピート/リピートの記録規則・遅延オープン(イベントゼロでファイル未作成)・degrade を検証する
  - 完了条件: ガード記録部品の単体テストが単独で緑になる
  - _Requirements: 4.1, 4.2, 4.3_
  - _Depends: 2.2_

- [x] 2.4 ランタイムへガードを結線する
  - ガード記録部品を debug 判定の外で常時生成し、停止・アンロード・再初期化で破棄する。debug 有効時は診断エンジンへガードログの保護ファイル名を渡す
  - マニフェスト受信時に送信元情報と config の期待識別子を採否判定へ伝搬し、結果で分岐する: 採用 → 従来どおり UI 適用、識別子不一致 → ガード記録のみ(採用済み計画・表示キャッシュは不変)、その他の拒否 → 従来どおり
  - セッション開始時、既存のクライアント別適用に加えてガード行を再発行する(採用計画なしの早期 return より前に実行)
  - 単体テストで debug 無効でも記録が動く結線・送信元伝搬・セッション開始時再発行・破棄を検証する
  - 完了条件: ランタイム単体テストが単独で緑になり、不一致マニフェスト受信で UI 再生成と表示キャッシュ更新が発生しない
  - _Requirements: 3.3, 3.4, 3.5, 3.6, 4.1, 4.3_

- [ ] 3. mock-unity をプロジェクト識別子に対応させる(バッチ B)
- [x] 3.1 (P) シナリオへプロジェクト識別子を追加する
  - シナリオ定義スキーマに識別子を必須追加し、生成するマニフェスト JSON に含める
  - 既定シナリオへ「osc-surface-demo」を設定し、不正マニフェストシナリオにも識別子を追加する(生マニフェスト上書きの挙動は不変)
  - 誤接続模擬シナリオ(別の識別子 + 既定と異なるエントリ構成)を新規作成し、E2E が UI の非上書きをエントリ差分で検証できるようにする
  - 単体テストで識別子の必須検証と生成 JSON への反映を検証する
  - 完了条件: シナリオ単体テストが単独で緑になり、誤接続シナリオが別識別子のマニフェストを生成する
  - _Requirements: 5.1, 5.2_
  - _Boundary: ScenarioRuntime_
  - _Depends: 1_

- [ ] 3.2 CLI 上書きと起動時の自発送信を実装する
  - 識別子を上書きする CLI フラグをシナリオ指定必須の従属フラグとして追加する(既存のキャラクター名フラグと同型・同じエラーパターン)
  - READY 行のペイロードへ解決済み識別子を追加し、テストから起動状態を観測できるようにする
  - ソケット待受開始後、Unity の OnEnable 相当としてマニフェストを 1 回自発送信する(要求前送信はプロトコル上許容済み)
  - 単体テストでフラグ制約・READY 出力・起動時自発送信を検証する
  - 完了条件: mock-unity の CLI 単体テストが単独で緑になり、起動しただけで識別子入りマニフェストが届く
  - _Requirements: 5.1, 5.2_

- [ ] 4. バッチ全体の単体テストを緑化する(バッチ D)
  - タスク 1〜3 の横断影響で残存する既存単体テストの追随(採否結果の判別 union 化への追従・テスト用 config やフィクスチャへの識別子追加など)を完了する
  - 完了条件: ワークスペース全体の vitest が全件緑になる(E2E はタスク 5.2 で回収)
  - _Requirements: 5.6, 5.7_

- [ ] 5. レイアウト・設定・E2E を更新する(バッチ E)
- [ ] 5.1 (P) 診断レイアウトと既定設定を更新する
  - 診断パネルへ誤接続ガード表示行(初期値「-」)を既存の診断行の定義パターンで追加する
  - リポジトリ既定の実行時設定へ期待識別子「osc-surface-demo」を追加する(既定でガード有効)
  - 完了条件: headless 起動で診断パネルにガード行が表示され、既定設定がスキーマ検証を通過する
  - _Requirements: 3.1, 4.3_
  - _Boundary: DiagnosticsLayout / Config_
  - _Depends: 1_

- [ ] 5.2 誤接続ガードの E2E を実装する
  - 一致採用: 期待識別子を設定した config + 既定シナリオで動的 UI が生成されることを確認する
  - 不一致拒否: 正しい mock で採用後、誤接続シナリオの第 2 mock を別の待受ポートで起動し、起動時自発送信された不一致マニフェストで UI が置き換わらないこと・ガード NDJSON に拒否行が記録されること・パネルのガード行が更新されることを確認する(debug 無効 config で実施し常時記録を検証)
  - 未設定採用: 期待識別子なしの一時 config で照合なしに採用されることを確認し、既存ループバック E2E の追随(識別子入りシナリオのまま緑)も行う
  - 完了条件: `corepack pnpm test`(vitest 単体 + Playwright E2E)が全件緑になる
  - _Requirements: 5.3, 5.4, 5.5, 5.7_

- [ ] 6. Unity 参照実装をアセット駆動へ移行する(バッチ F)
- [ ] 6.1 マニフェスト定義の ScriptableObject を実装する
  - プロジェクト識別子と操作エントリ一覧(アドレス・ラベル・型・ウィジェット・範囲・判別子方式の既定値・グループ)を保持するアセット定義を、作成メニュー付きで実装する(データ保持のみでロジックは持たない)
  - .meta の GUID は Editor(uloop 経由)生成を第一候補とし、手書きする場合は必ずランダムな 32 桁 hex を新規生成する(連続・ローテーションパターン禁止)
  - 完了条件: Unity でコンパイルが通り、インスペクタからアセットを作成・編集できる
  - _Requirements: 1.1, 1.3_

- [ ] 6.2 Bridge をアセット読み込みと検証付き送信に変更する
  - ハードコード配列とその型定義を削除し、アセット参照へ置き換える。現在値の充填・トークン置換・値記録・型照合はアセットのエントリ走査で従来と同じ意味論を維持する
  - OnEnable の自発送信とマニフェスト要求への応答の両方で、アセットの内容から識別子入り JSON をシリアライズして送信する(手書きビルダ方式を踏襲、enum 未知値はエラーで送信中止)
  - アセット未割当・識別子が空・エントリ不正のときはエラーをログ出力してマニフェスト送信を行わない(ping/stats/エコーバックは動作継続)
  - 完了条件: コンパイルが通り、有効なアセット割り当て時に version・projectId・entries を含むマニフェストが自発送信される
  - _Requirements: 1.1, 1.4, 1.5_

- [ ] 6.3 同梱アセットを作成し Scene へ割り当てる
  - 移行前のハードコード定義 5 件と同一内容 + 識別子「osc-surface-demo」の定義アセットを、Scene/Prefab 非埋め込みの単独ファイル(Force Text による YAML)として作成する
  - サンプル Scene の Bridge コンポーネントへ同梱アセットを割り当てる(uloop 経由の Editor 操作を第一候補、失敗時は YAML 直編集で GUID 参照を追記)
  - 完了条件: 実 Unity から移行前と等価(+識別子)のマニフェストが送信されて Surface 側で UI が生成され、アセットが Git 差分管理可能なテキストとしてコミットされている
  - _Requirements: 1.2, 1.6_

- [ ] 7. プロトコル仕様書と検証手順を整合させる(バッチ G)
- [ ] 7.1 プロトコル仕様本文へ識別子仕様を反映する
  - §2 のスキーマへ識別子(必須・空でない・人間が決める任意文字列)を追記し、誤接続ガードの新規小節(config の期待識別子・検証通過後の厳密比較・不一致時は不採用で採用済み UI 継続・debug 非依存の常時記録・値エコーバックは防がない制限・スキーマ不正の拒否では状態保護されない制限)を追加する
  - §4.3 の擬似コードへ識別子の出力とアセット未割当時の送信中止を反映する
  - 互換性ノートへ「識別子は JSON ペイロード内フィールドで OSC 型タグは s 1 引数のまま」「照合は Unicode 正規化なしのバイト等価比較」の判断を記録する
  - 完了条件: 仕様書のみを読んで任意の OSC ライブラリで識別子・照合・制限を含む互換実装を書ける状態になっている
  - _Requirements: 6.1, 6.3_

- [ ] 7.2 付録 A を 2 ファイル構成へ再構成する
  - 参照実装の掲載を Bridge とアセット定義の C# 2 ファイル全文構成にし、内容一致不変条件を「各 C# ファイルとリポジトリ実ファイルの全文一致」へ再定義する。対応表も 2 ファイル構成へ更新する
  - 同梱アセットは YAML 抜粋の例示として掲載し、スクリプト参照 GUID がプロジェクト固有のため不変条件の対象外であることを明記する
  - 完了条件: 付録 A の C# 全文がタスク 6 完了時点のリポジトリ実ファイルと一致している
  - _Requirements: 6.2_

- [ ] 7.3 (P) 手動検証手順を追記し全体回帰を確認する
  - 検証手順書へ Phase 5 の手動検証手順(識別子一致で UI 生成 / 不一致でパネル・NDJSON に拒否が記録され UI 不変 / アセット未割当時のエラーと送信停止 / アセット編集・差し替えでマニフェスト内容が変わる)を追記する
  - vendor(lockfile 含む)に差分がないことを git 差分で確認し、テスト一式が緑であることを確認して Phase 進捗を更新する
  - 7.1・7.2 とは触れるファイルが重ならないため並行可能
  - 完了条件: テスト一式が緑、vendor 無改造が確認でき、手動検証手順が追記されている
  - _Requirements: 6.4_
  - _Boundary: VERIFICATION.md_

## 順序制約(Migration Strategy 由来)

- タスク 1〜4 は update-together バッチ(A〜D)。中間状態では mock-unity と既存 E2E が壊れるため、各タスクの完了条件は「担当コンポーネントの単体テストが単独で緑」とし、全体緑はタスク 4(vitest)とタスク 5.2(E2E 含む全件)で確認する
- タスク 6(Unity 側)は mock-unity により Surface 側受け口が検証済みのため TS 側と独立に検証可能だが、設計のフロー(E → F → G)に従い E2E 緑化後に実施する
- タスク 7.2(付録 A)はタスク 6 の最終コード確定後にのみ更新する(内容一致不変条件)
