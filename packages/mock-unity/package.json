{
  "name": "@osc-surface/mock-unity",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=dist/mock-unity.js --external:osc",
    "typecheck": "tsc -p .",
    "test": "vitest run --project unit"
  },
  "dependencies": {
    "@osc-surface/shared": "workspace:*",
    "osc": "^2.4.5"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
