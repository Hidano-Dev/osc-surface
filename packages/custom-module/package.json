{
  "name": "@oscdesk/custom-module",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=dist/osc-surface.js",
    "typecheck": "tsc -p .",
    "test": "vitest run --project unit"
  },
  "dependencies": {
    "@oscdesk/shared": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
